"use client";

import { useRef,useState,useTransition } from "react"; import { useRouter } from "next/navigation"; import { useForm } from "react-hook-form"; import { zodResolver } from "@hookform/resolvers/zod"; import { z } from "zod"; import { ImagePlus } from "lucide-react"; import { toast } from "sonner";
import { photoSchema } from "@/lib/schemas"; import { savePhotoAction } from "@/actions/app"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Textarea } from "@/components/ui/textarea"; import { Dialog,DialogContent,DialogDescription,DialogHeader,DialogTitle,DialogTrigger } from "@/components/ui/dialog"; import { Field,selectClass } from "@/components/ui/form-field"; import { FormActions } from "./shared";
import { toDateInput } from "@/lib/utils";
import { DateInput } from "@/components/ui/date-input";
type Values=z.infer<typeof photoSchema>;
export function PhotoForm({logs,disabled=false}:{logs:{id:string;title:string}[];disabled?:boolean}){const[open,setOpen]=useState(false);const[pending,startTransition]=useTransition();const fileRef=useRef<HTMLInputElement>(null);const router=useRouter();const{register,handleSubmit,formState:{errors}}=useForm<Values>({resolver:zodResolver(photoSchema),defaultValues:{title:"",notes:"",date:toDateInput(new Date()),dailyLogId:""}});function submit(v:Values){const file=fileRef.current?.files?.[0];if(!file){toast.error("请选择一张图片");return}const fd=new FormData();Object.entries(v).forEach(([k,val])=>fd.set(k,String(val??"")));fd.set("image",file);startTransition(async()=>{const r=await savePhotoAction(fd);if(r.ok){toast.success("照片已收进回忆里");setOpen(false);router.refresh();}else toast.error(r.error);});}return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button variant="warm" disabled={disabled}><ImagePlus className="size-4"/>上传照片</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>收藏一个瞬间</DialogTitle><DialogDescription>支持 JPG、PNG、WebP，单张最大 10MB。</DialogDescription></DialogHeader><form onSubmit={handleSubmit(submit)} className="space-y-4"><Field label="选择图片"><Input ref={fileRef} type="file" accept="image/jpeg,image/png,image/webp" className="pt-3"/></Field><div className="grid gap-4 sm:grid-cols-2"><Field label="标题"><Input placeholder="今天的好天气" {...register("title")}/></Field><Field label="日期" error={errors.date?.message}><DateInput {...register("date")}/></Field></div><Field label="关联日常记录"><select className={selectClass} {...register("dailyLogId")}><option value="">不关联</option>{logs.map(x=><option key={x.id} value={x.id}>{x.title}</option>)}</select></Field><Field label="备注"><Textarea placeholder="这一刻为什么特别…" {...register("notes")}/></Field><FormActions pending={pending} onCancel={()=>setOpen(false)}/></form></DialogContent></Dialog>}
