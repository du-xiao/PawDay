"use client";

import { useState,useTransition } from "react"; import { useRouter } from "next/navigation"; import { useForm } from "react-hook-form"; import { zodResolver } from "@hookform/resolvers/zod"; import { z } from "zod"; import { Pencil,Plus } from "lucide-react"; import { toast } from "sonner";
import { healthSchema } from "@/lib/schemas"; import { saveHealthAction } from "@/actions/app"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Textarea } from "@/components/ui/textarea"; import { Dialog,DialogContent,DialogDescription,DialogHeader,DialogTitle,DialogTrigger } from "@/components/ui/dialog"; import { Field,selectClass } from "@/components/ui/form-field"; import { FormActions } from "./shared";
import { toDateInput } from "@/lib/utils";
type Values=z.infer<typeof healthSchema>;const types=["疫苗","驱虫","体检","用药","疾病","绝育","体重"] as const;
export function HealthForm({initial,disabled=false}:{initial?:Values;disabled?:boolean}){const[open,setOpen]=useState(false);const[pending,startTransition]=useTransition();const router=useRouter();const{register,handleSubmit,watch,formState:{errors}}=useForm<Values>({resolver:zodResolver(healthSchema),defaultValues:initial||{type:"体重",title:"日常称重",date:toDateInput(new Date()),notes:"",weightKg:"",nextReminderDate:""}});const type=watch("type");function submit(v:Values){startTransition(async()=>{const r=await saveHealthAction(v);if(r.ok){toast.success(initial?"健康记录已更新":"健康记录已保存");setOpen(false);router.refresh();}else toast.error(r.error);});}return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button variant={initial?"ghost":"warm"} size={initial?"sm":"default"} disabled={disabled}>{initial?<Pencil className="size-3.5"/>:<Plus className="size-4"/>}{initial?"编辑":"新增健康记录"}</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>{initial?"编辑健康记录":"新增健康记录"}</DialogTitle><DialogDescription>重要日期和下一次提醒会一起出现在首页。</DialogDescription></DialogHeader><form onSubmit={handleSubmit(submit)} className="space-y-4"><div className="grid gap-4 sm:grid-cols-2"><Field label="类型"><select className={selectClass} {...register("type")}>{types.map(x=><option key={x}>{x}</option>)}</select></Field><Field label="日期"><Input type="date" {...register("date")}/></Field></div><Field label="标题" error={errors.title?.message}><Input placeholder="例如：年度体检" {...register("title")}/></Field>{(type==="体重"||initial?.weightKg)&&<Field label="体重（kg）"><Input type="number" step="0.01" placeholder="5.20" {...register("weightKg")}/></Field>}<Field label="下次提醒日期" hint="可选"><Input type="date" {...register("nextReminderDate")}/></Field><Field label="备注"><Textarea placeholder="医院、剂量、医生建议…" {...register("notes")}/></Field><FormActions pending={pending} onCancel={()=>setOpen(false)}/></form></DialogContent></Dialog>}
