export function PageHeader({ eyebrow, title, description, action }: { eyebrow?: string; title: string; description: string; action?: React.ReactNode }) {
  return <div className="mb-5 flex flex-col gap-4 sm:mb-7 sm:flex-row sm:items-end sm:justify-between sm:gap-5"><div>{eyebrow && <p className="mb-1.5 text-[11px] font-semibold uppercase tracking-[.18em] text-[var(--orange)] sm:mb-2 sm:text-xs">{eyebrow}</p>}<h1 className="text-2xl font-semibold tracking-[-.035em] sm:text-4xl sm:tracking-[-.045em]">{title}</h1><p className="mt-1.5 max-w-2xl text-sm leading-relaxed text-[var(--muted)] sm:mt-2 sm:text-base">{description}</p></div>{action && <div className="w-full min-w-0 sm:w-auto [&>button]:w-full sm:[&>button]:w-auto">{action}</div>}</div>;
}
